requestAnimationFrame("dotenv").config();
var keys = require("/Users/ashleyhaven/Desktop/liri-node-app/keys.js");
var spotify = new spotify(keys.spotify);
var song = function (concert) {
    this.concert = this.concert;
}
